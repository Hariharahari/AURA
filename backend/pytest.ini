[pytest]
# Tell pytest exactly where our tests live
testpaths = tests

# Tell pytest NEVER to look inside these folders
norecursedirs = cloned_repos faiss_dbs reports prod_repo .venv node_modules

# Add the current directory to the Python path so tests can find main.py
pythonpath = .